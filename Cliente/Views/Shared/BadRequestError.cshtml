@model Cliente.Models.VMs.Errors.BadRequestViewModel

@{
    ViewData["Title"] = "¡Roja!";
}

<div class="row text-white">
    <div class="col-12 granite text-center p-3">
        <h1>¡Roja directa!</h1>
        <figure>
            <img src="./img/redcard.png" alt="Imagen de error" />
        </figure>
        <h2>Parece que quedaste en offside...</h2>
    </div>
    <div class="col-12 granite p-3">
        <dl>
            <dt>
                @Html.DisplayNameFor(model => model.Message)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Message)
            </dd>
        </dl>
    </div>
</div>

<div class="row mt-3 px-3">
    <a class="btn btn-outline-dark btn-block" asp-action="Index" asp-controller="Admin">Al panel de Admin</a>
</div>

